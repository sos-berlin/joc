* Required fields are
    * path
    * objectType
    * account
    * deployed
    * configurationDate
    * configuration

The configuration contains the JobScheduler Object in JSON representation
