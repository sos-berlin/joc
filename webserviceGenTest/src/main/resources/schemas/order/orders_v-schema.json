{
  "title": "orders with delivery date (volatile part)",
  "id": "schemas/order/orders_v",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "required": [
    "deliveryDate",
    "orders"
  ],
  "properties": {
    "deliveryDate": {
      "$ref": "../common/deliveryDate-schema.json"
    },
    "orders": {
      "type": "array",
      "items": {
        "$ref": "order_v-schema.json"
      }
    }
  }
}
