{
  "title": "job modify",
  "id": "schemas/job/modifyRuntime",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "array",
  "minItems": 1,
  "items": {
    "type": "object",
    "required": [
      "path",
      "runTime"
    ],
    "properties": {
      "path": {
        "$ref": "../common/path-schema.json"
      },
      "comment": {
        "description": "Field to comment manually job modifications which can be logged.",
        "type": "string"
      },
      "runTime": {
        "description": "A run_time xml is expected which is specified in the <xsd:complexType name='run_time'> element of  http://www.sos-berlin.com/schema/scheduler.xsd",
        "type": "string"
      }
    }
  }
}
