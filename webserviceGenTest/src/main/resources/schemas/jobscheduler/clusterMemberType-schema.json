{
  "title": "jobscheduler cluster member type",
  "id": "schemas/jobscheduler/clusterMemberType",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "required": [
    "type"
  ],
  "properties": {
    "type": {
      "description": "JobScheduler doesn't run in a cluster (standalone) or is member of an active (distributed orders) or passive cluster (backup)",
      "type": "string",
      "enum": [
        "active",
        "passive",
        "standalone"
      ],
      "javaEnumNames": [
        "active",
        "passive",
        "standalone"
      ]
    },
    "precedence": {
      "description": "primary (0), secondary (1), ...only relevant for passive cluster",
      "type": "integer",
      "minimum": 0
    }
  }
}
