{
  "title": "agents with delivery date (volatile part)",
  "id": "schemas/jobscheduler/agents_v",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "required": [
    "deliveryDate",
    "agents"
  ],
  "properties": {
    "deliveryDate": {
      "$ref": "../common/deliveryDate-schema.json"
    },
    "agents": {
      "type": "array",
      "items": {
        "$ref": "agent_v-schema.json"
      }
    }
  }
}
