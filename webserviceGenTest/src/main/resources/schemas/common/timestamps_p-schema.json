{
  "title": "delivery and survey date for permanent",
  "id": "schemas/common/timestamps_p",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "description": "timestamps of permanent requests",
  "required": [
    "deliveryDate",
    "surveyDate"
  ],
  "properties": {
    "deliveryDate": {
      "$ref": "../common/timestamp-schema.json"
    },
    "surveyDate": {
      "$ref": "../common/surveyDate_p-schema.json"
    }
  }
}
