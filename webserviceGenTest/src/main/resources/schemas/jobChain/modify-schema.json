{
  "title": "stop or unstop command",
  "id": "schemas/jobChain/modify",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "the command is part of the web servive url",
  "type": "object",
  "required": [
    "jobschedulerId"
  ],
  "properties": {
    "jobschedulerId": {
      "type": "string"
    },
    "jobChains": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "jobChain"
        ],
        "properties": {
          "jobChain": {
            "$ref": "../common/path-schema.json"
          },
          "comment": {
            "description": "Field to comment this action which can be logged.",
            "type": "string"
          }
        }
      }
    }
  }
}
